# DispatchKit Runbooks

Operational procedures and incident response.

## Naming

Use `incident-<short-name>.md` for incident response (e.g., `incident-dispatch-core-outage.md`). Use `runbook-<short-name>.md` for routine procedures.

## Index

*(TODO: runbooks pending. The current escalation path lives in the internal handbook — separate system.)*
